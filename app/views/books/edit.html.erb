<head></head>
<body>

    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

    <h2>Editing Book</h2>
    <!--<!--%= form_with model: @book, url: update_book_path(@book.id), method: :patch do |f| %>-->
    <% if @book.errors.any? %>
            <h3><%= @book.errors.count %>errors prohibited this obj from being saved:</h3>
            <ul>
              <% @book.errors.full_messages.each do |message| %>
                <li><%= message %></li>
                <% end %>
                </ul>
              <% end %>

    <%= form_with model: @book do |f| %>
          <label for="book_title">Title</label>
          <br><%= f.text_field :title, autofocus: true, id:"inputTitle", placeholder:"Title"%>
          <br><label for="book_opinion">Opinion</label>
          <br><%= f.text_field :body, autofocus: true, id:"inputBody", placeholder:"Opinion"%>

  <br><%= f.submit "Update Book", class:"btn btn-success" %>
  <br><%= link_to "Show", book_path %>
  |
      <%= link_to "Back", books_path %>
  <!--<!--% end %>-->
  <% end %>
  </div>
</body>
